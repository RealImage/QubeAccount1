# Qube Account

Internal access-management admin portal implementing the Company Management
control plane described in `Qube_Account_Spec_Draft_v0.2.md`.

See `CLAUDE.md` for stack, structure, and the assumptions this build makes
about the spec's open questions.

## Getting started

```bash
npm install
npm run dev
```
